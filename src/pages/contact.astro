---
import PageContent from "./[...lang]/contact.astro";

// Default to 'en' if 'lang' is undefined
const defaultLang = "en";
const { lang = defaultLang } = Astro.props;
console.log("services root Resolved language:", lang);
---

<PageContent lang={lang} />